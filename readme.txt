Se requiere instalar las librerías pygame y numpy.

Desde la terminal de Windows
pip install pygame
pip install numpy

Para ingresar el número de generaciones a calcular de clic en el recuadro gris que contiene el número 0.
Para iniciar la ejecución de clic en el botón play.

